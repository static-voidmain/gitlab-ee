[gitlab-nginx-git-http]
# 정상 Git 요청을 오탐하므로 활성화하지 않는다. GitLab 내장 Rack Attack 설정을 사용한다.
enabled = false
filter = gitlab-nginx-git-http
logpath = /var/log/nginx/gitlab_access.log
maxretry = 5
findtime = 600
bantime = 3600
backend = auto
