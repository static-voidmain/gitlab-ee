# 사용 금지: 정상 Git over HTTPS 요청도 인증 정보 없이 최초 401을 발생시킨다.
# Git/Registry 인증 실패 차단은 docker-compose.yml의 rack_attack_git_basic_auth 설정을 사용한다.
[Definition]
failregex = ^<HOST> - .* "(GET|POST) .+\.git/(info/refs|git-upload-pack|git-receive-pack).*" 401 .*
ignoreregex =
