# 개요(Overview)

너는 한국 금융권의 특히 카드사 DevOps 전문가야. 금감원에서 제안하는 보안사항을 보수적으로 적용하여 서비스가 유지되고 구성되고 있어. 아래 열거되는 내용들을 참고해서 GitLab EE의 최신안정화 버전으로 `docker-compose.yml`파일 및 필요한 파일들을 자동으로 구성 또는 제작해줘.
`docker-compose.yml` 및 필요 파일을 생성시 아래 내용 적용 사항에 대해서 주석으로 어떤 요구사항, 어떤 구현상세, 어떤 제약사항 저리를 위해서 코드 생성을 했는지에 대한 내용 추가할 수 있도록 한다.

# 요구사항(Requirement)

- 안정화된 소스코드 관리(`SCM`)를 위하여 `Self-hosted` 기준의 `GitLab EE`을 최신안정화 버전으로 제공
- 기본적인 `OWSAP`, `금감원`, `금결원` 등에서 제안하는 보안가이드를 준수
  - 금감원, 금결원 등에서 제안하는 보안가이드는 추후 감사 등을 대비해 최대한 보수적으로 접근 및 해석하여 `GitLab EE` 구성
- 단일 호스트 서버에 `Docker` 기반으로 구성 및 운영 할 수 있도록 제공 필요
- 운영, 유지보수(보안패치, Disk 추가, artifact 관리 등) 등이 용이하도록 구성 필요
- 가장 표준적인 일반적인 기준으로 `GitLab EE`를 구성

# 구현상세(Implementation Detail)

- `GitLab EE` 를 위해서 별도의 reverse proxy가 적용된 `Nginx` 를 제공
  - `Nginx`는 이미 구성어어 되어있기 떄문에 `docker-compose.yml` 파일 작성시 고려하지 않아도 됨
- `GitLab EE` 를 사용하는 사용자는 90일마다 비밀번호를 바꾸도록 해야함
- `GitLab EE` 를 사용하는 사용자의 비밀번호는 `소문자 + 대문자 + 숫자 + 특수문자` 등을 최소 각 1개씩 포함해야하며, 최소 글자는 12글자
- `GitLab EE` 에 사용자/비밀번호로 로그인하는 경우 5회 이상 틀릴시 계정이 잠그도록 설정할 수 있어야함(필수 X)
- `GitLab EE` 의 `GitLab Documenation` 같이 제공할 수 있도록 구성
- `GitLab Runner`를 사용
  - 취약점이 적으면서도 구성하기 간편한 방법으로 `GitLab EE <-> GitLab Runner`를 구성
  - `runner`를 위한 이미지는 아래와 같이 준비
    - `maven(jdk 7, 8, 17, 21)`
    - `gradle(jdk 7, 8, 17, 21)`
    - `jdk 7, 8, 17, 21`
    - `npm`
    - `pnpm`
    - `shell`
  - `runner`를 통해서 만들어진 artifact(`*.war`, `*.jar`)는 배포를 위해 별도로 만들어진 `Nexus`에 업로드 하도록 구성
    - 배포를 위해 만들어진 `Nexus`는 개발, 검증, 운영을 위한 별도의 `repository` 제공
    - 향 후 배포를 위해 만들어진 `Nexus`는 artifact를 컨테이너화 하여 harbour로 변경예정
    - 배포용 `Nexus`는 이미 구성어어 되어있기 떄문에 `docker-compose.yml` 파일 작성시 고려하지 않아도 됨
  - `shell runner`를 통해서 다양한 코드 확인 자동화가 가능해야 한다
    - `shell runner`로는 `secure code check(SAST)`를 확인할 수 있는 사내 구성된 `Coderay 6.0 RG` 에 `CLI`를 통해서 확인요청
      - `Secure code check` 확인요청을 `CLI`를 통해서 하는경우 응답 내용을 `MR` 댓글로 달 수있도록
      - `MR`에 commit 이 일어나면 이전 응답내용 댓글을 삭제 후, `MR` 재 댓글
      - `CLI`를 통해서 `SAST` 요청하는 경우 로컬 환경에 텍스트 파일로 점검 내용의 서머리가 저장됨 해당 텍스트를 읽어 MR 댓글, 재댓글
    - `shell runner`를 통해서 `code linting` 을 할 수 있도록 해야함
      - `code linting`은 `check style` 을 통해서 진행(정의된 \*.xml 파일 제공)
      - 결과 값을 MR 댓글로 제공 필요
    - `shell runner`를 통해서 `code coverage` 진행 할 수 있도록 해야함
      - `code coverage` 실행 결과를 `MR` 댓글로 달 수 있도록 해야함
      - `code coverage` 는 `GitLab EE`의 `Merge Requests` 설정의 `Approval rule`에 `code coverage` 활성화 한 것과 연동 될 수 있도록 하여. MR의 `code coverage` 결과에 따라 `merge` 에 허용될 수 있어야 한다
    - 위의 `shell runner` 로 제안된 내용중 `shell runner`가 필요 없는 경우 구현 가능한 방법을 제안해도 좋다
  - `GitLab Runner` 는 이미지별로 각 5~10개 정도가 `parallel` 하게 동작할 수 있어야 한다.
- `docker hub` 에서 제공되지 않는 컨테이너 이미지는 직접 이미지를 제작할 수있도록 `DockerFile` 파일 추가 제작 및 제안 필요

# 제약사항(Constraint)

- docker 이미지는 `scout quickview`로 `scan` 했을 때, 취약점 중 `critical`, `high` 가 없거나 적어야 한다
- 이미지는 `arm64`가 아닌 `amd64`로 제한
- 보안에 취약한 부분은 지양
- GitLab EE의 라이선스는 `Premium`으로 구매헀음. `Ultimate` 기능은 지양

# 테스트 통과(Acceptance Tests)

- docker 이미지 취약점 결과도출
- 최대한 취약점 등을 피했다 하더라도 가능한 취약점 예측 및 도출
- 취약점 대처를 위한 보안방안 제안
- 해킹 위협이 되는 부분을 찾아 테스트 필요
- 생성해주는 파일이 정상적으로 동작되는지 등을 검증 필요
  - 필요에 따라서는 간단한 샘플 프로젝트 및 코드를 만들고 MR 생성 가능한 범위 내에서 테스트 진행
